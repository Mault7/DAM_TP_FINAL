import { FechaPipePipe } from './fecha-pipe.pipe';

describe('FechaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FechaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
